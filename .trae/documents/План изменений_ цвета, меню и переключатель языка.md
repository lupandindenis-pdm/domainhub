Я выполню следующие изменения в соответствии с вашими требованиями:

1.  **Смягчение красного цвета для статусов:**
    *   Обновлю переменную `--status-expired` в файле `src/index.css`. Сейчас она использует яркий красный (`0 72% 51%`). Я заменю его на более мягкий пастельный оттенок (`0 85% 75%`), аналогичный тому, что мы сделали для других ошибок.

2.  **Сокращение пунктов меню в сайдбаре:**
    *   В файле `src/components/language-provider.tsx` изменю переводы:
        *   `"Реестр доменов"` → `"Реестр"`
        *   `"История изменений"` → `"История"`

3.  **Изменение логики переключателя языка:**
    *   В файле `src/components/language-toggle.tsx` изменю отображаемый текст на кнопке.
    *   Вместо *текущего* языка будет отображаться тот, на который можно *переключиться* (если сейчас RU — покажет EN, и наоборот).

После внесения изменений я проверю сборку и отправлю обновления на сервер.